GLU_META={'n_warm':20,   # Default minimum number of data points before fitting
           'freq':'H',    # Sampling frequency used if none is supplied, or t not supplied
           'n_max':1000}   # Maximum length of training data

GLU_DEEP_MODEL = {}